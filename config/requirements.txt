# requirements.txt
# Dependencies pentru PDF-to-Embedding Kaggle processing pipeline
#
# Instalare: pip install -r config/requirements.txt
#
# Nota: PyMuPDF și PaddleOCR pot lua 2-3 minute să download (mari size)

# PDF Processing
# PyMuPDF - Extract text din PDFs 10x mai rapid decât PyPDF2
PyMuPDF>=1.23.0
PyMuPDF-fonts>=1.0.5  # Optional: fonts support

# OCR - Optical Character Recognition
# PaddleOCR - Extrage text din imagini (suportă 80+ limbi inclusiv română)
paddleocr>=2.7.0
# PaddlePaddle se instalează automat cu paddleocr (nu trebuie manual)

# Embeddings - Semantic vector generation
# sentence-transformers - BERT multilingual embeddings
sentence-transformers>=2.2.2

# Database
# supabase-py - Client Supabase pgvector
supabase>=2.0.0
postgrest-py>=0.10.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0  # Pentru similarity calculations

# Utilities
pyyaml>=6.0  # YAML config file support
python-dotenv>=1.0.0  # Load env variables din .env

# Progress & Logging
tqdm>=4.65.0  # Progress bars
# python-logging-loki>=0.3.2  # Optional: log centralization (disabled - not needed)

# Optional: GPU acceleration
# Uncomment dacă vrei CUDA support (pentru Kaggle cu GPU)
# torch>=2.0.0
# torchvision>=0.15.0
# pytorch-cuda>=11.8

# Development (optional)
pytest>=7.0  # Unit testing
black>=23.0  # Code formatting
pylint>=2.17.0  # Linting
mypy>=1.0  # Type checking
